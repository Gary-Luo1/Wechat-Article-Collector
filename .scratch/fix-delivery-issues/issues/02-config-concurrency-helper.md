# 02 — 配置并发写保护：锁与事务助手

**What to build:** 配置文件的读-改-写变为事务化。两个进程并发修改配置时，后写者基于最新状态执行，不再用旧副本覆盖已失效的授权或丢失并发修改；健康状态更新也走同一通道。

**Blocked by:** None — can start immediately.

**Status:** completed (2026-08-01, tests/test_config_store.py 4 passed, incl. cross-process lock test)

- [ ] 提供跨平台配置锁与「持锁加载→修改→校验→保存」的事务助手，写入仍原子。
- [ ] 健康状态更新迁移到事务助手。
- [ ] 并发回归测试：一方失效授权后，另一方的陈旧提交不会复活授权；并发健康更新与政策变更最终状态一致。
- [ ] 既有行为无变化，全量测试通过。
