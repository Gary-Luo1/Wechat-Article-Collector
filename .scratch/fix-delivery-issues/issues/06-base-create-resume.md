# 06 — Base 创建部分成功后续跑

**What to build:** 远端 Base 已创建但后续步骤（经理授予、字段检查）失败时，重试同一命令从断点继续：不重复创建远端 Base、不被「目标已存在」守卫阻断，最终完成授权、校验、字段映射与启用。

**Blocked by:** 02, 03

**Status:** completed (2026-08-01, tests/test_create_base_resume.py 2 passed)

- [ ] 已保存的「创建中」状态（含 Base/表标识）可被识别为断点并续跑，创建只发生一次。
- [ ] 后续步骤幂等可重入；最终状态为已启用 + 字段映射已保存 + 授权已消费。
- [ ] 对真正已配置的既有目标仍拒绝创建第二个 Base。
- [ ] 回归测试覆盖授予失败后重试；全量测试通过。
