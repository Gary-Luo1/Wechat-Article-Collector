# 03 — 状态目录规则单一来源

**What to build:** 「技能状态与 venv 放在哪个目录」只有一份平台规则；命令运行时与存储模块永远指向同一位置，边界环境（如状态目录环境变量为空字符串）下两者也不会分家。

**Blocked by:** 01

**Status:** done

- [x] 命令运行时改为消费唯一路径模块，删除重复实现
- [x] 现有 venv 路径跟随测试通过，行为不变
- [x] 新增边界测试：状态目录环境变量为空字符串时回退正确
- [x] 全量回归通过：217 passed, 3 skipped
