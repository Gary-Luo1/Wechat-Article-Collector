# 02 — URL 身份规则收敛为单一模块

**What to build:** 文章 URL 的校验、规范化、去重键与 http 升级只有一份实现；纯字符串的 URL 校验逻辑不再要求安装网页解析库。用户粘贴链接入库、发现去重、飞书记录 URL 全部使用同一套身份规则，行为与现状一致。

**Blocked by:** 01

**Status:** done

- [x] 新增只依赖标准库的 URL 身份模块（不得 import 网页解析库）
- [x] 原解析器模块、队列模块、发现格式化与飞书记录构造改用该模块；对外行为逐字节不变
- [x] 阻塞解析器模块导入时，URL 身份模块全部函数仍可用（有测试证明）
- [x] 未安装解析库的环境下，纯 URL 允许列表测试可以通过
- [x] 全量回归通过：217 passed, 3 skipped
