# 04 — 队列与存储模块深化（汇总接口 + 单一文件锁）

**What to build:** 待处理/已处理文章的统计汇总与「哪些 URL 已在队列中」的查询只由队列模块提供，诊断报告与发现流程不再直接读取队列存储结构；同时队列与配置两份存储共用同一套跨平台文件锁。用户看到的 doctor/status 数字与重构前完全一致，两个存储的并发安全行为保持不变。

**Blocked by:** 01, 02

**Status:** done

- [x] 队列模块提供汇总与已知 URL 查询接口，字段与现有收件箱查询的汇总一致
- [x] 诊断报告改用该接口，输出 JSON 与重构前逐字段一致（快照回归）
- [x] 发现流程的去重改用该接口，行为不变
- [x] 直接单元测试覆盖空队列与各类标记组合
- [x] 单一锁模块实现（含超时行为），队列与配置存储改为调用
- [x] 双进程并发写入回归通过（配置事务）；队列并发回归通过
- [x] 新增锁超时路径测试
- [x] 全量回归通过：217 passed, 3 skipped
