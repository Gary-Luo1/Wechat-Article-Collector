interface:
  display_name: "WeChat Article Subscriber"
  short_description: "Ingest, score, and sync WeChat articles"
  default_prompt: "Use $wechat-article-subscriber to front-load my configuration and approvals, then automatically discover, score, and sync WeChat articles within that policy."
